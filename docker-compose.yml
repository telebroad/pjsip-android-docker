version: '3.8'

services:
  pjsip-android:
    build:
      context: .  # Path to your Dockerfile
      dockerfile: Dockerfile  # Name of your Dockerfile
    image: pjsip-android-docker:latest  # Name of the image to build
    container_name: pjsip-android-docker  # Name of the container
    volumes:
      - ./data:/pjsip/data  # Mount local 'data' directory to 'data' in the container
      - ./build:/pjsip/build
    environment:
      - a=1

